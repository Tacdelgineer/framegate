# Required queue connection and worker identity
JOB_QUEUE_URL=http://100.123.208.90:8787
DGX_WORKER_ID=aitopatom-c85a

# Proposed defaults for the separately synchronized worker implementation
DGX_POLL_INTERVAL=2
DGX_WORK_ROOT=/var/tmp/content-factory-worker

# Worker-owned model selections; confirm these when the DGX files arrive.
TTS_MODEL=
TRANSCRIBE_MODEL=
